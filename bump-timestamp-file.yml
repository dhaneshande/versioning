---
platform: linux

image_resource:
  type: docker-image
  source: { repository: concourse/bosh-cli }

inputs:
  - name: resource-tutorial

run:
  path: bash
  args:
    - -e
    - -c
    - |
      ls -l
      set -o pipefail
      chmod +x resource-tutorial/bump-timestamp-file.sh
      pwd
      ls -R
      ../resource-tutorial/bump-timestamp-file.sh
